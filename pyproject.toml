[project]
name = "ddd-learning-challenges"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "black>=25.0",
    "pre-commit>=4.0",
]

[tool.black]
line-length = 88
target-version = ["py312"]
